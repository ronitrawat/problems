# Write your MySQL query statement below
SELECT E.EMPLOYEE_ID FROM EMPLOYEES E LEFT JOIN EMPLOYEES M ON M.EMPLOYEE_ID=E.MANAGER_ID WHERE M.EMPLOYEE_ID IS NULL AND E.SALARY<30000 AND E.MANAGER_ID IS NOT NULL ORDER BY E.EMPLOYEE_ID;